#include "controls.h"

// Setup a ctrl
err setup_ctrl(Ctrl_setup* setup) {
    // TODO: Setup PWM for this pin

    return SUCC;
}

// Set Ctrl Throttle
err set_ctrl(uint pin, uint value) {
    // TODO: Set PWM value for pin

    return SUCC;
}

// Get Ctrl Throttle
uint get_ctrl(uint pin) {
    // TODO: Get PWM value for pin
    return 0;
}